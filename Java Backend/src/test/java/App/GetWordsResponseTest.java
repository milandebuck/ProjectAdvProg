package App;

import junit.framework.TestCase;

/**
 * Created by Domien on 22-11-2016.
 */
public class GetWordsResponseTest extends TestCase {
        public void testAnswerChecker(){
            assertEquals(true,true);
            //String word = "het vliegtuig";
            //String answer = "plane";
            //GetWordsResponse response = new GetWordsResponse();
            //EntryRepository repository = null;
            //assertEquals(true, response.checkResponse(repository, word, answer));
        }
}
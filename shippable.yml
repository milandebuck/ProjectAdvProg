#shippable config file
#made and maintained by Team Martini

#language settings
language: java
#jdktag
jdk:
	- oraclejdk8

services:
  - mongodb

build:
  ci:
    - mongo projectDB --eval 'db.addUser("user1", "Azerty123");'